function _id(id) {
    return document.getElementById(id);
}

function _cl(element) {
    return document.querySelector(element);
}

function _all(element) {
    return document.querySelectorAll(element);
}